import SwiftUI

enum AppColor {
    // MARK: - Base / Surface

    static let backgroundSky = Color(hex: "#C8FBFF")
    static let surface = Color(hex: "#FFF7E8")
    static let surfaceSecondary = Color(hex: "#DEECCC")
    static let surfaceMint = Color(hex: "#EAFBF5")
    static let borderSoft = Color(hex: "#BFE7E3")

    // MARK: - Text

    static let textPrimary = Color(hex: "#2F3550")
    static let textSecondary = Color(hex: "#445378")
    static let textMuted = Color(hex: "#6C7893")
    static let textOnDark = Color(hex: "#FFF7E8")

    // MARK: - Primary / Accent

    static let primaryBlue = Color(hex: "#48B2C6")
    static let primaryHover = Color(hex: "#409E94")
    static let primarySoft = Color(hex: "#82E5CE")

    static let manualYellow = Color(hex: "#F8EE80")
    static let manualOrange = Color(hex: "#E68F39")

    // MARK: - Status

    static let success = Color(hex: "#59CB9A")
    static let successDark = Color(hex: "#058670")
    static let warning = Color(hex: "#E68F39")
    static let danger = Color(hex: "#B85743")
    static let dangerSoft = Color(hex: "#F3C0B5")
    static let infoSoft = Color(hex: "#DDF7FB")

    // MARK: - Game Theme

    static let aiBlue = Color(hex: "#48B2C6")
    static let aiBlueSoft = Color(hex: "#DDF7FB")
    static let aiCyanGlow = Color(hex: "#82E5CE")

    static let obedience = Color(hex: "#445378")
    static let humanity = Color(hex: "#59CB9A")
    static let humanitySoft = Color(hex: "#EAFBF5")

    static let glitchPurple = Color(hex: "#8E63C9")
    static let candyPink = Color(hex: "#F58BB5")

    // MARK: - Aliases

    static let creamWhite = Color(hex: "#FFF7E8")
    static let charcoalNavy = Color(hex: "#2F3550")
    static let buttonSurface = Color(hex: "#FFF8EE")
    static let buttonInk = Color(hex: "#202326")

    // MARK: - App Palette Aliases

    static let appSkyBlue = Color(hex: "#C8FBFF")
    static let appSoftAqua = Color(hex: "#82E5CE")
    static let appMintGreen = Color(hex: "#59CB9A")
    static let appDeepTeal = Color(hex: "#058670")
    static let appSand = Color(hex: "#E1CE91")
    static let appWoodTan = Color(hex: "#D6B074")
    static let appWarmBrown = Color(hex: "#C27052")
    static let appDarkBrick = Color(hex: "#B85743")
    static let appSlateBlue = Color(hex: "#445378")
    static let appBrightBlue = Color(hex: "#48B2C6")
    static let appFlameOrange = Color(hex: "#E68F39")
    static let appCandyPink = Color(hex: "#F58BB5")
    static let appGrapePurple = Color(hex: "#8E63C9")
}
